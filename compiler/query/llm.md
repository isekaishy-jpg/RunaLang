# Query

- Keep query families split by semantic concern.
- Session owns caches; query exposes semantic access.
- Cache failures and cycles, not only successes.
- Prefer stable ids over names or spans.
