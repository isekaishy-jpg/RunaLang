# AST Notes
- Keep `root.zig` small and stable.
- Put structured syntax payloads in sibling files.
- AST may carry temporary compatibility text during parser rewrite.
- New parser truth should enter through structured `syntax`.
